apiVersion: v1
kind: Service
metadata:
    name: josecarlosanpedrokubernetesnodejs
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: josecarlosanpedrokubernetesnodejs